# parser_test 待办事项

## 当前状态

**整体完成度：约 15%**

这是一个最小可行的测试工具，仅用于最基本的演示。

| 功能 | 状态 | 完成度 |
|------|------|--------|
| 基本解析演示 | ✅ 完成 | 100% |
| 命令行参数支持 | ❌ 未实现 | 0% |
| 文件读取 | ❌ 未实现 | 0% |
| 友好的 AST 输出 | ❌ 未实现 | 0% |
| 测试覆盖 | ❌ 无 | 0% |

---

## 🟡 中优先级

### 1. 支持命令行参数
- [ ] 使用 clap 或简单的 std::env::args 解析参数
- [ ] 支持 `--file <path>` 从文件读取
- [ ] 支持 `--source <code>` 直接传入代码字符串
- [ ] 支持 `--help` 显示帮助

### 2. 改进 AST 输出格式
- [ ] 实现自定义的 AST 美化打印，不使用 Debug
- [ ] 适当的缩进和换行
- [ ] 可选的彩色高亮
- [ ] 支持 `--json` 输出格式（使用 serde_json）

### 3. 改进错误显示
- [ ] 导入并使用 `x-cli` 中的 `pipeline::format_parse_error`（或复制逻辑）
- [ ] 显示错误位置的源码片段
- [ ] 显示行号和列号

---

## 🟢 低优先级

### 4. 功能增强
- [ ] 支持 `--emit tokens` 先显示分词结果再显示 AST
- [ ] 支持 `--stat` 输出统计信息（声明数、表达式数等）
- [ ] 支持 `--only-decls` 只显示顶层声明

### 5. 批量测试
- [ ] 支持目录遍历，测试多个 .x 文件
- [ ] 生成测试报告（成功/失败计数）

### 6. 单元测试
- [ ] 测试参数解析
- [ ] 测试 AST 格式化

---

## 测试覆盖率估计

当前无测试。
