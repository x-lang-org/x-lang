# X 语言示例

本目录包含两类材料：

- 可作为当前仓库功能演示入口的基础/常规示例
- 为未来能力预留的概念性或占位示例

如果你想优先查看与当前工具链验证路径更一致的示例，建议先从这些文件开始：

- `hello.x`
- `fib.x`
- `basics/`
- `types/`
- `functional/`
- `oop/`
- `modules/`

## 占位示例

以下文件目前仍明确带有 `coming soon` 提示，应理解为能力方向展示或概念草图，而不是已经被完整证明的端到端能力：

- `concurrency/async_await.x`
- `concurrency/channels.x`
- `practical/file_io.x`
- `practical/http_server.x`

阅读这些文件时，请以“说明目标方向”为主，而不要把它们当作当前编译器、运行时或标准库成熟度的完整证据。
