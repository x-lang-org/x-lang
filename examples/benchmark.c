#ifdef _WIN32
// 尝试链接uuid.lib，但如果不存在也不会报错
#pragma comment(lib, "uuid.lib")
#endif

// Generated by X-Lang C23 backend
// DO NOT EDIT

#include "x_runtime.h"

XValue x_init_database(void);
XValue x_json_handler(XValue req);
XValue x_plaintext_handler(XValue req);
XValue x_db_handler(XValue req, XValue pool);
XValue x_db_handler_wrapper(XValue req);
XValue x_queries_handler(XValue req, XValue pool);
XValue x_queries_handler_wrapper(XValue req);
XValue x_updates_handler(XValue req, XValue pool);
XValue x_updates_handler_wrapper(XValue req);
XValue x_fortunes_handler(XValue req, XValue pool);
XValue x_fortunes_handler_wrapper(XValue req);

XValue db_config = {.tag = X_NULL, .as = {.i = 0}};
XValue db_pool = {.tag = X_NULL, .as = {.i = 0}};
XValue app = {.tag = X_NULL, .as = {.i = 0}};
XValue template_engine = {.tag = X_NULL, .as = {.i = 0}};
XValue static_config = {.tag = X_NULL, .as = {.i = 0}};
XValue static_handler = {.tag = X_NULL, .as = {.i = 0}};
XValue server = {.tag = X_NULL, .as = {.i = 0}};
XValue x_init_database(void) {
    XValue conn = x_get_connection(db_pool);
    if (x_as_bool(x_is_some(conn))) {
        XValue connection = x_unwrap(conn);
        x_execute(connection, x_string("CREATE TABLE IF NOT EXISTS world (id INT PRIMARY KEY, randomNumber INT)"), x_list());
        /* TODO: for loop */
        x_execute(connection, x_string("CREATE TABLE IF NOT EXISTS fortune (id INT PRIMARY KEY, message VARCHAR(255))"), x_list());
        XValue fortunes = x_list();
        XValue fortune1 = x_map();
        x_builtin_map_set(fortune1, x_string("id"), x_int(1LL));
        x_builtin_map_set(fortune1, x_string("message"), x_string("fortune: No such file or directory"));
        XValue fortune2 = x_map();
        x_builtin_map_set(fortune2, x_string("id"), x_int(2LL));
        x_builtin_map_set(fortune2, x_string("message"), x_string("A computer scientist is someone who fixes things that aren't broken"));
        XValue fortune3 = x_map();
        x_builtin_map_set(fortune3, x_string("id"), x_int(3LL));
        x_builtin_map_set(fortune3, x_string("message"), x_string("After enough decimal places, nobody gives a damn"));
        XValue fortune4 = x_map();
        x_builtin_map_set(fortune4, x_string("id"), x_int(4LL));
        x_builtin_map_set(fortune4, x_string("message"), x_string("A bad random number generator: 1, 1, 1, 1, 1, 4.33e+67, 1, 1, 1"));
        XValue fortune5 = x_map();
        x_builtin_map_set(fortune5, x_string("id"), x_int(5LL));
        x_builtin_map_set(fortune5, x_string("message"), x_string("A computer program does what you tell it to do, not what you want it to do"));
        XValue fortune6 = x_map();
        x_builtin_map_set(fortune6, x_string("id"), x_int(6LL));
        x_builtin_map_set(fortune6, x_string("message"), x_string("Emacs is a nice operating system, but I prefer UNIX. — Tom Christaensen"));
        XValue fortune7 = x_map();
        x_builtin_map_set(fortune7, x_string("id"), x_int(7LL));
        x_builtin_map_set(fortune7, x_string("message"), x_string("Any program that runs right is obsolete"));
        XValue fortune8 = x_map();
        x_builtin_map_set(fortune8, x_string("id"), x_int(8LL));
        x_builtin_map_set(fortune8, x_string("message"), x_string("A list is only as strong as its weakest link. — Donald Knuth"));
        XValue fortune9 = x_map();
        x_builtin_map_set(fortune9, x_string("id"), x_int(9LL));
        x_builtin_map_set(fortune9, x_string("message"), x_string("Feature: A bug with seniority"));
        XValue fortune10 = x_map();
        x_builtin_map_set(fortune10, x_string("id"), x_int(10LL));
        x_builtin_map_set(fortune10, x_string("message"), x_string("Computers make very fast, very accurate mistakes"));
        XValue fortune11 = x_map();
        x_builtin_map_set(fortune11, x_string("id"), x_int(11LL));
        x_builtin_map_set(fortune11, x_string("message"), x_string("<script>alert('This should not be displayed in a browser alert box.')</script>"));
        XValue fortune12 = x_map();
        x_builtin_map_set(fortune12, x_string("id"), x_int(12LL));
        x_builtin_map_set(fortune12, x_string("message"), x_string("フレームワークのベンチマーク"));
        XValue fortune13 = x_map();
        x_builtin_map_set(fortune13, x_string("id"), x_int(13LL));
        x_builtin_map_set(fortune13, x_string("message"), x_string("Делать то, что нужно, а не то, что можно"));
        XValue fortune14 = x_map();
        x_builtin_map_set(fortune14, x_string("id"), x_int(14LL));
        x_builtin_map_set(fortune14, x_string("message"), x_string("Benchmarking frameworks is hard"));
        XValue fortune15 = x_map();
        x_builtin_map_set(fortune15, x_string("id"), x_int(15LL));
        x_builtin_map_set(fortune15, x_string("message"), x_string("You never get away from that which you are afraid of"));
        x_push(fortunes, fortune1);
        x_push(fortunes, fortune2);
        x_push(fortunes, fortune3);
        x_push(fortunes, fortune4);
        x_push(fortunes, fortune5);
        x_push(fortunes, fortune6);
        x_push(fortunes, fortune7);
        x_push(fortunes, fortune8);
        x_push(fortunes, fortune9);
        x_push(fortunes, fortune10);
        x_push(fortunes, fortune11);
        x_push(fortunes, fortune12);
        x_push(fortunes, fortune13);
        x_push(fortunes, fortune14);
        x_push(fortunes, fortune15);
        /* TODO: for loop */
        x_return_connection(db_pool, connection);
    } else {
        x_print(x_string("Failed to connect to database"));
    }
    return x_null();
}

XValue x_json_handler(XValue req) {
    XValue data = x_map();
    x_builtin_map_set(data, x_string("message"), x_string("Hello, World!"));
    XValue headers = x_map();
    x_builtin_map_set(headers, x_string("Content-Type"), x_string("application/json; charset=utf-8"));
    x_build_response(x_int(200LL), x_to_json(data), headers);
    return x_null();
}

XValue x_plaintext_handler(XValue req) {
    XValue headers = x_map();
    x_builtin_map_set(headers, x_string("Content-Type"), x_string("text/plain; charset=utf-8"));
    x_build_response(x_int(200LL), x_string("Hello, World!"), headers);
    return x_null();
}

XValue x_db_handler(XValue req, XValue pool) {
    XValue conn = x_get_connection(pool);
    if (x_as_bool(x_is_some(conn))) {
        XValue connection = x_unwrap(conn);
        XValue id = x_add(x_random(x_int(10000LL)), x_int(1LL));
        XValue result = x_query(connection, x_string("SELECT id, randomNumber FROM world WHERE id = ?"), x_list_1(id));
        XValue w = x_map();
        x_builtin_map_set(w, x_string("id"), id);
        if (x_as_bool(x_is_some(result))) {
            x_builtin_map_set(w, x_string("randomNumber"), x_random(x_int(10000LL)));
        } else {
            x_builtin_map_set(w, x_string("randomNumber"), x_int(42LL));
        }
        XValue world = w;
        x_return_connection(pool, connection);
        XValue headers = x_map();
        x_builtin_map_set(headers, x_string("Content-Type"), x_string("application/json; charset=utf-8"));
        x_build_response(x_int(200LL), x_to_json(world), headers);
    } else {
        XValue headers = x_map();
        x_builtin_map_set(headers, x_string("Content-Type"), x_string("application/json; charset=utf-8"));
        XValue error = x_map();
        x_builtin_map_set(error, x_string("error"), x_string("Database connection failed"));
        x_build_response(x_int(500LL), x_to_json(error), headers);
    }
    return x_null();
}

XValue x_db_handler_wrapper(XValue req) {
    x_db_handler(req, db_pool);
    return x_null();
}

XValue x_queries_handler(XValue req, XValue pool) {
    XValue queries = x_int(1LL);
    if (x_as_bool(x_builtin_map_contains(x_index_get(req, x_string("query_params")), x_string("queries")))) {
        XValue q = x_builtin_map_get(x_index_get(req, x_string("query_params")), x_string("queries"));
        (queries = x_to_number(q));
        if (x_as_bool(x_lt(queries, x_int(1LL)))) {
            (queries = x_int(1LL));
        } else {
            if (x_as_bool(x_gt(queries, x_int(500LL)))) {
                (queries = x_int(500LL));
            }
        }
    }
    XValue conn = x_get_connection(pool);
    if (x_as_bool(x_is_some(conn))) {
        XValue connection = x_unwrap(conn);
        XValue worlds = x_list();
        /* TODO: for loop */
        x_return_connection(pool, connection);
        XValue headers = x_map();
        x_builtin_map_set(headers, x_string("Content-Type"), x_string("application/json; charset=utf-8"));
        x_build_response(x_int(200LL), x_to_json(worlds), headers);
    } else {
        XValue headers = x_map();
        x_builtin_map_set(headers, x_string("Content-Type"), x_string("application/json; charset=utf-8"));
        XValue error = x_map();
        x_builtin_map_set(error, x_string("error"), x_string("Database connection failed"));
        x_build_response(x_int(500LL), x_to_json(error), headers);
    }
    return x_null();
}

XValue x_queries_handler_wrapper(XValue req) {
    x_queries_handler(req, db_pool);
    return x_null();
}

XValue x_updates_handler(XValue req, XValue pool) {
    XValue queries = x_int(1LL);
    if (x_as_bool(x_builtin_map_contains(x_index_get(req, x_string("query_params")), x_string("queries")))) {
        XValue q = x_builtin_map_get(x_index_get(req, x_string("query_params")), x_string("queries"));
        (queries = x_to_number(q));
        if (x_as_bool(x_lt(queries, x_int(1LL)))) {
            (queries = x_int(1LL));
        } else {
            if (x_as_bool(x_gt(queries, x_int(500LL)))) {
                (queries = x_int(500LL));
            }
        }
    }
    XValue conn = x_get_connection(pool);
    if (x_as_bool(x_is_some(conn))) {
        XValue connection = x_unwrap(conn);
        XValue worlds = x_list();
        x_begin_transaction(connection);
        /* TODO: for loop */
        x_commit(connection);
        x_return_connection(pool, connection);
        XValue headers = x_map();
        x_builtin_map_set(headers, x_string("Content-Type"), x_string("application/json; charset=utf-8"));
        x_build_response(x_int(200LL), x_to_json(worlds), headers);
    } else {
        XValue headers = x_map();
        x_builtin_map_set(headers, x_string("Content-Type"), x_string("application/json; charset=utf-8"));
        XValue error = x_map();
        x_builtin_map_set(error, x_string("error"), x_string("Database connection failed"));
        x_build_response(x_int(500LL), x_to_json(error), headers);
    }
    return x_null();
}

XValue x_updates_handler_wrapper(XValue req) {
    x_updates_handler(req, db_pool);
    return x_null();
}

XValue x_fortunes_handler(XValue req, XValue pool) {
    XValue conn = x_get_connection(pool);
    if (x_as_bool(x_is_some(conn))) {
        XValue connection = x_unwrap(conn);
        XValue result = x_query(connection, x_string("SELECT id, message FROM fortune"), x_list());
        XValue f1 = x_map();
        x_builtin_map_set(f1, x_string("id"), x_int(1LL));
        x_builtin_map_set(f1, x_string("message"), x_string("Hello, World!"));
        XValue f2 = x_map();
        x_builtin_map_set(f2, x_string("id"), x_int(2LL));
        x_builtin_map_set(f2, x_string("message"), x_string("Welcome to X Language"));
        XValue fortunes = x_list();
        if (x_as_bool(x_is_some(result))) {
            XValue f3 = x_map();
            x_builtin_map_set(f3, x_string("id"), x_int(3LL));
            x_builtin_map_set(f3, x_string("message"), x_string("Benchmarking is fun"));
            (fortunes = x_list_3(f1, f2, f3));
        } else {
            (fortunes = x_list_2(f1, f2));
        }
        x_return_connection(pool, connection);
        XValue context = x_map();
        x_builtin_map_set(context, x_string("fortunes"), fortunes);
        XValue content = x_render_template(template_engine, x_string("fortune"), context);
        XValue headers = x_map();
        x_builtin_map_set(headers, x_string("Content-Type"), x_string("text/html; charset=utf-8"));
        x_build_response(x_int(200LL), content, headers);
    } else {
        XValue headers = x_map();
        x_builtin_map_set(headers, x_string("Content-Type"), x_string("text/html; charset=utf-8"));
        x_build_response(x_int(500LL), x_string("Database connection failed"), headers);
    }
    return x_null();
}

XValue x_fortunes_handler_wrapper(XValue req) {
    x_fortunes_handler(req, db_pool);
    return x_null();
}

int main(int argc, char **argv) {
    // Initialize runtime
    x_runtime_init();
    
    // Initialize database
    x_init_database();
    
    // Start server
    x_print(x_string("Server starting on port 8080..."));
    // Initialize server config
    XValue router = x_router();
    x_get(router, x_string("/json"), x_json_handler);
    x_get(router, x_string("/plaintext"), x_plaintext_handler);
    x_get(router, x_string("/db"), x_db_handler_wrapper);
    x_get(router, x_string("/queries"), x_queries_handler_wrapper);
    x_get(router, x_string("/updates"), x_updates_handler_wrapper);
    x_get(router, x_string("/fortunes"), x_fortunes_handler_wrapper);
    
    XValue server_config = x_map();
    x_builtin_map_set(server_config, x_string("host"), x_string("127.0.0.1"));
    x_builtin_map_set(server_config, x_string("port"), x_int(8080LL));
    x_builtin_map_set(server_config, x_string("router"), router);
    x_builtin_map_set(server_config, x_string("middleware"), x_middleware_chain());
    x_builtin_map_set(server_config, x_string("thread_pool_size"), x_int(4LL));
    
    XValue server = x_server(server_config);
    x_start(server);
    
    // Cleanup
    x_runtime_cleanup();
    return 0;
}
