# x-lexer TODO

## 当前状态

- 基本功能已实现
- 支持主要语言关键字和字面量
- 提供完整的位置信息（Span）

## 待办事项

### 1. 字符串字面量处理
- [x] 完善字符串转义字符支持（\n, \t, \\, \", \', \0 等）
- [ ] 实现多行字符串字面量解析（后续）
- [ ] 支持字符串插值（后续）
- [ ] 处理Unicode字符转义（后续）

### 2. 字符字面量处理
- [x] 实现完整的字符字面量解析
- [x] 支持转义字符（\n \t \r \' \\ \0）
- [ ] 支持Unicode字符（后续）

### 3. 数字字面量处理
- [x] 实现二进制、八进制、十六进制数字解析（0b/0o/0x）
- [x] 支持数字分隔符（如 1_000_000）
- [x] 完善浮点数解析（科学计数法等，已有）

### 4. 错误处理
- [x] 未闭合字符串返回 UnclosedString、未闭合字符返回 UnclosedChar、非法数字返回 InvalidNumber
- [ ] 优化错误信息的精确性（后续）
- [ ] 实现更好的错误恢复机制
- [ ] 支持多错误报告（后续）

### 5. 性能优化
- [ ] 优化词法分析器的性能
- [ ] 支持增量词法分析
- [ ] 减少内存分配

### 6. 测试覆盖
- [x] 添加单元测试模块
- [x] 测试所有边界条件（空输入、空白、注释、字符串/字符/数字、错误分支、peek/last_span）
- [ ] 性能基准测试（后续）

### 7. 其他
- [ ] 支持shebang处理
- [ ] 处理BOM（字节顺序标记）
- [ ] 完善文档和示例

## 质量门禁（可测试与可验证）

### 覆盖率目标

- **行覆盖率**：100%
- **分支覆盖率**：100%
- **测试通过率**：100%

### 必须具备的测试类型

- [x] **单元测试**：覆盖 `Lexer` 的状态机分支（Normal）与错误分支（未闭合字面量、非法数字等）
- [x] **边界条件**：空输入、仅空白、注释与字符串/字符/数字组合、peek 与 last_span
- [ ] **回归用例**：为每个已修复的 lexer bug 添加最小复现用例（按需补充）

### 验收步骤（本地一键验证）

```bash
cd compiler
cargo test -p x-lexer

# 覆盖率（line/branch）
cargo llvm-cov -p x-lexer --tests
```

## 完成度估计

**整体完成度**：约 80%
- 基本功能：95%
- 高级功能：75%（缺多行字符串入口、插值、Unicode 转义）
- 错误处理：70%（正确错误类型，缺多错误收集）
- 测试覆盖：75%（单元测试覆盖主要路径与错误，可进一步用 llvm-cov 提升）

**当前测试覆盖**：本 crate 含 28 个单元测试，覆盖关键字、标识符、数字（含 0x/0o/0b）、字符串与字符及转义、未闭合与非法数字错误、Span、边界与 peek/last_span。`cargo test -p x-lexer` 全过；可用 `cargo llvm-cov -p x-lexer --tests` 查看覆盖率。
