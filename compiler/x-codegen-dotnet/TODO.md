# x-codegen-dotnet TODO List

## 项目概览

x-codegen-dotnet 是 X 语言的 .NET 后端，负责将 X 语言程序编译为 .NET CIL 代码。

## 当前状态

**阶段**：早期阶段（桩实现）

**当前覆盖范围**：0%

- 代码生成器框架已实现
- 所有代码生成方法均返回未实现错误
- 需要完整实现 CIL 代码生成逻辑

## 待办事项

### 1. 核心基础设施 (高优先级)

- [ ] 实现类型映射：X 语言类型 → .NET 类型
- [ ] 实现基本块代码生成
- [ ] 实现函数调用代码生成
- [ ] 实现变量绑定和访问

### 2. 语言特性支持

#### 基础类型

- [ ] 整数类型支持 (i8, i16, i32, i64, isize)
- [ ] 浮点类型支持 (f32, f64)
- [ ] 布尔类型支持 (bool)
- [ ] 字符和字符串支持 (char, string)

#### 控制流

- [ ] if/else 语句
- [ ] while/for 循环
- [ ] 模式匹配

#### 函数与变量

- [ ] 函数定义和调用
- [ ] 变量声明和赋值
- [ ] 闭包支持

### 3. 高级特性

- [ ] 类和接口支持
- [ ] 数组和集合类型
- [ ] 异常处理
- [ ] 泛型类型
- [ ] 异步/等待支持

### 4. 优化与调试

- [ ] 代码优化（常量折叠、死代码消除等）
- [ ] 调试信息生成 (PDB 文件)
- [ ] 性能优化

### 5. 测试与验证

- [ ] 单元测试：为每个代码生成方法编写测试
- [ ] 集成测试：与编译器流水线集成测试
- [ ] 基准测试：与其他后端性能对比
- [ ] 验证测试：确保生成的代码正确性

### 6. 文档与示例

- [ ] API 文档完善
- [ ] 使用示例
- [ ] 常见问题解答

## 质量门禁（可测试与可验证）

### 覆盖率目标

- **行覆盖率**：100%
- **分支覆盖率**：100%
- **测试通过率**：100%

### 必须具备的测试类型

- [ ] **单元测试**：覆盖 CIL 生成的每个核心分支（类型映射、控制流、调用、变量生命周期）与错误分支（当前为桩，先建立骨架）
- [ ] **快照/Golden 测试**：对生成的 IL 文本（或等价表示）做稳定比对
- [ ] **执行验证**：生成程序集后跑最小程序（需要 .NET 运行时/工具链）
- [ ] **集成测试**：经 `tools/x-cli` 触发 dotnet 目标生成，端到端验证输出

### 验收步骤（本地一键验证）

```bash
cd compiler
cargo test -p x-codegen-dotnet

# 覆盖率（line/branch）
cargo llvm-cov -p x-codegen-dotnet --tests
```

## 完成标准

### 第一阶段（基本功能）

- 支持核心语言特性
- 能够编译并运行简单程序
- 基础调试信息生成
- 编译过程无错误

### 第二阶段（完善功能）

- 支持所有主要语言特性
- 优化编译输出
- 完善测试覆盖
- 性能达到可接受水平

### 第三阶段（优化成熟）

- 高级优化技术
- 完整的调试支持
- 与 .NET 生态系统深度集成
- 性能优化至最佳水平

## 备注

本项目是 X 语言编译器的重要组成部分，需要与其他子项目保持同步。实现过程中应遵循 X 语言设计目标。
