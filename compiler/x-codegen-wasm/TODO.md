# x-codegen-wasm TODO List

## 项目概览

x-codegen-wasm 是 X 语言编译器的 WebAssembly 绑定，允许在浏览器或 Node.js 环境中使用。

## 当前状态

**阶段**：早期阶段（功能不全）

**当前覆盖范围**：30%

- 已实现基本的 WebAssembly 绑定框架
- 支持调用 TypeScript 代码生成器
- 类型检查功能尚未启用
- 需要完善 JavaScript 编译和其他功能

## 待办事项

### 1. 核心功能完善 (高优先级)

- [ ] 启用并完善类型检查
- [ ] 实现完整的 JavaScript 编译功能
- [ ] 优化编译过程的错误处理
- [ ] 提高编译性能和内存管理

### 2. 语言特性支持

- [ ] 确保所有 X 语言特性在 Web 环境中正常工作
- [ ] 支持生成 Web 友好的代码
- [ ] 处理 Web 平台特有的限制

### 3. API 设计

- [ ] 设计更符合 Web 开发习惯的 API
- [ ] 添加异步编译支持
- [ ] 提供编译进度和状态反馈
- [ ] 支持编译选项配置

### 4. 错误处理与调试

- [ ] 完善错误信息格式
- [ ] 添加错误位置和代码片段显示
- [ ] 支持编译过程的调试信息输出

### 5. 性能优化

- [ ] 优化 WebAssembly 模块大小
- [ ] 提高编译速度
- [ ] 优化内存使用
- [ ] 支持增量编译

### 6. 测试与验证

- [ ] 单元测试：为每个编译方法编写测试
- [ ] 集成测试：与浏览器和 Node.js 环境集成测试
- [ ] 功能测试：验证编译输出的正确性
- [ ] 性能测试：在不同浏览器中的性能测试

### 7. 文档与示例

- [ ] API 文档：详细的使用文档
- [ ] 示例代码：各种使用场景的示例
- [ ] 教程：从基础到高级的使用指南
- [ ] 常见问题解答

### 8. 构建与部署

- [ ] 优化构建流程
- [ ] 支持多种打包方式
- [ ] CI/CD 集成
- [ ] 版本管理策略

## 质量门禁（可测试与可验证）

### 覆盖率目标

- **行覆盖率**：100%
- **分支覆盖率**：100%
- **测试通过率**：100%

### 必须具备的测试类型

- [ ] **单元测试**：覆盖 wasm 导出 API（编译到 TS/JS 的入口、错误分支、配置分支）
- [ ] **绑定集成测试（Node）**：`wasm-pack test --node` 验证导出函数在 Node 环境可用
- [ ] **绑定集成测试（browser）**：按需增加 `wasm-pack test --headless --chrome|firefox`
- [ ] **回归用例**：每次修复 wasm 绑定/错误格式问题都新增最小复现

### 验收步骤（本地一键验证）

```bash
cd compiler
cargo test -p x-codegen-wasm

# 覆盖率（line/branch）
cargo llvm-cov -p x-codegen-wasm --tests

# wasm 包构建
cd x-codegen-wasm
wasm-pack build --target web
```

## 完成标准

### 第一阶段（基础功能）

- 支持基本语法编译到 TypeScript/JavaScript
- 类型检查功能正常工作
- 错误处理完善
- 编译过程稳定

### 第二阶段（完善功能）

- 所有 X 语言特性支持
- 编译性能达到可接受水平
- 内存管理优化
- 测试覆盖全面

### 第三阶段（成熟阶段）

- 高级编译优化
- 完整的调试支持
- 生态系统集成
- 与其他编译器后端功能对齐

## 备注

本项目需要与 X 语言其他子项目保持同步。实现过程中应特别关注 Web 平台的特性和限制。
