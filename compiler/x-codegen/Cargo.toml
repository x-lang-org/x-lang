[package]
name = "x-codegen"
version = "0.1.0"
edition = "2021"
description = "X语言代码生成器核心接口"
license = "MIT"

[features]
default = []

[dependencies]
x-lir = { workspace = true }

# 错误处理
thiserror = { workspace = true }

# 日志
log = { workspace = true }
