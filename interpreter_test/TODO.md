# interpreter_test 待办事项

## 当前状态

**整体完成度：约 15%**

这是一个最小可行的测试工具，仅用于最基本的演示。

| 功能 | 状态 | 完成度 |
|------|------|--------|
| 基本执行演示 | ✅ 完成 | 100% |
| 命令行参数支持 | ❌ 未实现 | 0% |
| 文件读取 | ❌ 未实现 | 0% |
| 输出捕获和显示 | ❌ 未实现 | 0% |
| 测试覆盖 | ❌ 无 | 0% |

---

## 🟡 中优先级

### 1. 支持命令行参数
- [ ] 使用 clap 或简单的 std::env::args 解析参数
- [ ] 支持 `--file <path>` 从文件读取
- [ ] 支持 `--source <code>` 直接传入代码字符串
- [ ] 支持 `--help` 显示帮助
- [ ] 支持 `-- <args...>` 传递参数给程序

### 2. 改进输出和结果显示
- [ ] 捕获并显示解释器的 print 输出
- [ ] 访问并显示 Interpreter 内部的变量环境
- [ ] 显示最终表达式的求值结果
- [ ] 支持 `--env` 选项打印执行后的变量环境
- [ ] 支持 `--trace` 选项显示执行追踪

### 3. 改进错误显示
- [ ] 使用带位置的解析错误显示
- [ ] 显示运行时错误的位置（如果解释器提供）
- [ ] 错误发生时显示部分变量环境帮助调试

---

## 🟢 低优先级

### 4. 功能增强
- [ ] 支持 `--emit ast` 先显示 AST 再执行
- [ ] 支持 `--step` 单步执行
- [ ] 支持 `--break <line>` 设置断点
- [ ] 简单的调试交互模式

### 5. 批量测试
- [ ] 支持目录遍历，测试多个 .x 文件
- [ ] 与 expected/ 目录对比输出
- [ ] 生成测试报告

### 6. 单元测试
- [ ] 测试参数解析
- [ ] 测试输出捕获

---

## 测试覆盖率估计

当前无测试。
