# lexer_test 待办事项

## 当前状态

**整体完成度：约 15%**

这是一个最小可行的测试工具，仅用于最基本的演示。

| 功能 | 状态 | 完成度 |
|------|------|--------|
| 基本分词演示 | ✅ 完成 | 100% |
| 命令行参数支持 | ❌ 未实现 | 0% |
| 文件读取 | ❌ 未实现 | 0% |
| 友好输出 | ❌ 未实现 | 0% |
| 测试覆盖 | ❌ 无 | 0% |

---

## 🟡 中优先级

### 1. 支持命令行参数
- [ ] 使用 clap 或简单的 std::env::args 解析参数
- [ ] 支持 `--file <path>` 从文件读取
- [ ] 支持 `--source <code>` 直接传入代码字符串
- [ ] 支持 `--help` 显示帮助

### 2. 改进输出格式
- [ ] 使用 `colored`  crate 给不同 Token 类型上色
- [ ] 显示行号和列号（使用 Span 的 line_col）
- [ ] 更清晰的 Token 名称（不使用 Debug 格式）
- [ ] 可选的 `--json` 输出格式

### 3. 错误处理增强
- [ ] 更详细的错误信息显示
- [ ] 显示错误位置的源码片段
- [ ] 建议修复（如适用）

---

## 🟢 低优先级

### 4. 批量测试
- [ ] 支持目录遍历，测试多个 .x 文件
- [ ] 生成测试报告
- [ ] 回归测试支持

### 5. 交互模式
- [ ] REPL 风格的交互分词
- [ ] 实时显示 Token 结果

### 6. 单元测试
- [ ] 测试参数解析
- [ ] 测试输出格式化

---

## 测试覆盖率估计

当前无测试。
