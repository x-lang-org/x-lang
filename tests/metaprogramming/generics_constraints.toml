# 元编程测试: 泛型约束
name = "metaprogramming_generics"
description = "测试泛型约束和特化"
category = "metaprogramming"
spec = ["spec/docs/10-metaprogramming.md"]

source = """
// 泛型约束尚未完全实现
// 简化测试：基本泛型函数
function identity<T>(x: T) -> T {
    x
}

println(identity(42))
"""

[expect]
compile = true

[expect.runtime]
output = "42\n"
