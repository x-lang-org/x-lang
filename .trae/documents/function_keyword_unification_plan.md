# 函数关键字统一为 function - 实现计划

## [x] 任务 1: 检查并修改 library/stdlib/json.x 文件
- **优先级**: P0
- **依赖**: 无
- **描述**: 将 json.x 文件中的 `fn` 关键字修改为 `function`
- **成功标准**: json.x 文件中所有函数定义都使用 `function` 关键字
- **测试要求**:
  - `programmatic` TR-1.1: 文件编译无错误
  - `human-judgement` TR-1.2: 代码符合统一的函数定义规范
- **备注**: 这是唯一使用 `fn` 关键字的文件

## [x] 任务 2: 验证所有 X 语言文件的函数关键字
- **优先级**: P1
- **依赖**: 任务 1
- **描述**: 再次搜索所有 X 语言文件，确保没有其他文件使用 `fn` 关键字
- **成功标准**: 所有 X 语言文件都使用 `function` 关键字定义函数
- **测试要求**:
  - `programmatic` TR-2.1: 搜索结果显示无 `fn` 关键字使用
  - `human-judgement` TR-2.2: 代码风格统一，符合规范
- **备注**: 确保没有遗漏任何文件

## [x] 任务 3: 编译测试
- **优先级**: P1
- **依赖**: 任务 2
- **描述**: 编译测试文件，确保修改后的代码能够正常编译
- **成功标准**: 测试文件编译成功，无语法错误
- **测试要求**:
  - `programmatic` TR-3.1: 编译命令执行成功
  - `human-judgement` TR-3.2: 编译输出无错误信息
- **备注**: 验证修改不会影响代码的正常编译
